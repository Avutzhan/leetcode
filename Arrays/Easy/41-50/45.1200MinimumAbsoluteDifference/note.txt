1200. Minimum Absolute Difference
Easy
Array

Учитывая массив различных целых чисел arr, найдите все пары
элементов с минимальной абсолютной разницей любых двух элементов.

Возвращает список пар в порядке возрастания
(относительно пар), каждая пара [a, b] следует за

a, b are from arr
a < b
b - a equals to the minimum
absolute difference of
any two elements in arr